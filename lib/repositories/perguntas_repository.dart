/*import 'dart:collection';

import 'package:flutter/material.dart';

class PerguntasRepository extends ChangeNotifier{

  List<Pergunta> _lista =[];

  UnmodifiableListView<Pergunta> get lista => UnmodifiableListView(_lista);

  saveall(List<Pergunta> perguntas){
    perguntas.forEach(pergunta){
      if(!_lista.contains(pergunta))_lista.add(pergunta);
    }
  };

  remove(Pergunta pergunta){
    _list.remove(pergunta);
    notifyListeners();
  }
}*/

